@model ClientManage.Domain.Entities.RoleInfo

@{
    ViewBag.Title = "Edit";
}

<h1>Edit @Model.RoleName</h1>

@using (Html.BeginForm("Edit", "Roles"))
{
    @Html.ValidationSummary()
    @Html.EditorForModel()
    <input type="submit" value="Save" />
    @Html.ActionLink("Cancel and return to list","List")
}
